{
  "name": "MyWebsite Codespace",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspaces/Assignment-7",
  "forwardPorts": [5000],
  "postCreateCommand": "pip install -r requirements.txt || true",
  "remoteUser": "vscode"
}
